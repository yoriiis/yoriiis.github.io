name: Build

on:
  merge_group:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: yoriiis/actions/.github/workflows/test-and-build.yml@main
    with:
      node-version: 12
      command-build: npm run build